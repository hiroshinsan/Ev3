{
  "private": true,
  "workspaces": [
    "packages/*",
    "example"
  ],
  "scripts": {
    "dev": "cd example && npm run dev",
    "build": "cd packages/components && npm run build"
  },
  "devDependencies": {
    "npm-run-all": "4.1.5"
  }
}